spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/em_bank_rest
    username: postgres
    password: 1
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
logging:
  level:
    sql: debug
    web: debug
management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include:
          - auditevents
          - beans
          - caches
          - conditions
          - configprops
          - env
          - health
          - heapdump
          - httptrace
          - info
          - logfile
          - loggers
          - liquibase
          - metrics
          - mappings
          - scheduledtasks
          - sessions
          - shutdown
          - startup
          - threaddump
app:
  jwt-secret: mominfactilovefrogsfrogsfrogsfrogsfrogsfrogs
  access-token-expiration: "#{1000 * 60 * 60}"
  refresh-token-expiration: "#{1000 * 60 * 60 * 24 * 7}"
