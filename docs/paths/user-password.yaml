patch:
  security:
    - bearerAuth:
        - USER
  tags:
    - user-controller
  operationId: updatePassword
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../components/schemas/user.yaml#/UpdatePasswordRequest"
    required: true
  responses:
    "400":
      description: Invalid fields
    "401":
      description: Unauthorized
    "200":
      description: OK
